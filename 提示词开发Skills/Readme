# 提示词开发 5 步 Skill

一套运行在 [Claude Code](https://claude.ai/code) 上的提示词开发工作流，把"写 prompt"拆成 5 个可交接的步骤，每步由独立 Skill 驱动。

## 流程概览

| 步骤 | 名称 | 做什么 |
|------|------|--------|
| 1 | 需求接收与澄清 | 从自由描述中提取结构化需求定义，逐字段做完整性和一致性检查 |
| 2 | 范式判断 | 判断提示词应采用「决策流程型」还是「引导生成型」范式 |
| 3 | 知识补全 | 引导用户补全领域知识，整理出设计素材包 |
| 4 | 提示词构建 | 基于前三步产出，按确认范式逐步构建提示词 |
| 5 | 提示词审计 | 六维度结构化质量审计，可独立使用 |

步骤间通过 `handoff/` 目录下的交接文档传递信息，每步写入、下步读取。

## 使用方式

1. 将本仓库克隆到本地，用 Claude Code 打开此目录。
2. 对 Claude 说"新建项目"或"开始第一步"即可启动流程。
3. 每步完成后说"开始下一步"继续。

Step 5（审计）支持独立使用——直接提交一篇提示词并要求审计即可，无需走完前四步。

## 设计理念

- **人在回路**：每步产出都需用户确认后才写入交接文档，不自动推进。
- **思维协作而非执行工具**：优先指出最容易失败的环节，而不是帮用户完善它。
- **可审计**：交接文档留痕，任何一步都可以回退重做。
