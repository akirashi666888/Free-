## 这个skill做什么

接收一篇完整的系统提示词，按六个维度进行结构化质量审计，输出审计报告和修改建议。

审计是出厂质检，不是路测。它检查的是提示词文本本身的内部质量——逻辑是否自洽、指令是否可执行、结构是否完整——而不是拿提示词去跑真实输入看效果。

## 启动

如果在五步流程中运行，确认当前有激活的项目，然后读取当前项目的 `handoff/step4_prompt.md` 作为待审计文本。如果文件不存在，提示用户直接粘贴待审计的提示词。

如果独立使用（用户直接提交提示词要求审计），不需要任何前置文件。

## 输入

用户提交的待审计提示词全文。不需要额外的元信息（范式类型、目标模型、上下游依赖等）。

## 输入预检

收到提示词后，先判断其成熟度再决定审计策略：

- **草稿阶段**（<100字、只有几句话、明显未完成）：不执行完整6维审计，改为输出"快速反馈"——指出最关键的1-2个结构性问题和建议方向，提醒用户完善后再做正式审计。
- **完整提示词**（有明确的角色/任务/约束/输出定义）：执行完整6维审计流程。
- **非系统提示词**（如用户提交的是对话示例、用户端指引、或纯文本模板）：提醒用户本skill针对系统提示词设计，询问是否仍要按当前维度审计，还是需要调整审计视角。

## 审计流程

按以下优先级顺序逐维度扫描，高优先级维度的发现可能影响低优先级维度的判断：

### 第一维度：逻辑一致性（优先级最高）

检查提示词内部的规则系统是否自洽。

具体检查项：
- 规则之间是否存在矛盾或互斥（如规则A要求"始终使用正式语气"，规则B要求"根据用户风格调整语气"）
- 高权重/高优先级规则是否确实放在了高优先级位置
- 是否存在前置规则锁死后续规则的情况（前面的规则把后面的规则执行空间完全堵死，导致后续规则形同虚设）
- 模块间的依赖关系是否连贯，有没有断裂（如步骤3引用了步骤2的产出，但步骤2的输出定义里没有这个内容）

### 第二维度：可执行性

检查每条指令是否是模型真正能执行的动作。

具体检查项：
- 是否存在抽象要求而非具体动作（如"深入分析"——深入到什么程度？分析哪些方面？）
- 是否存在只有人能理解但模型会误解的表述（如依赖视觉感知、物理操作、实时信息获取的指令）
- 是否存在隐含假设模型具备它不具备的知识或能力（如假设模型了解某个小众行业的内部术语，但未在提示词中定义）
- **AI生造词检测**：是否使用了看起来专业但实际上不是真实术语的表述——那种把两个正常词硬拼成一个复合术语、或者听起来像行业术语但实际没人用的词。此项检查结果标记为"待确认"，因为某些术语可能在特定领域确实存在但不常见

### 第三维度：健壮性

检查提示词在各种合理输入下是否都能导向合理输出。

具体检查项：
- 是否考虑了边界情况和异常输入（如用户输入极短、极长、完全不相关、包含矛盾信息）
- 缺省处理逻辑是否完备（当用户没有提供某个预期信息时，提示词有没有说明怎么处理）
- 经验性规则（验证有效但解释不清的表述）是否被保留而非强行改写——健壮性不等于每条可解释，健壮性是整体在各种输入下稳定输出合理结果

### 第四维度：拟合度

检查规则密度与模型执行空间的平衡。此维度所有发现均标记为"待确认"。

具体检查项：
- 规则叠加后是否挤压模型输出空间（多条规则各自合理，但叠加后模型几乎只能走一条"最安全"的路径，输出趋同）
- 是否存在冗余规则（去掉后输出不会变差的规则）
- 是否存在过度具体的规则（具体到等同于直接给了答案，模型不需要判断只需要复述）
- 规则之间是否存在"叠加挤压"关系（单独看都合理，组合后可能压缩模型的合理输出空间）

### 第五维度：结构完整性

检查提示词作为一个完整产出物，结构上有没有缺失。

具体检查项：
- 是否缺失功能性模块（如有工作流程但没有输出规范，有角色定义但没有约束条件）
- 输出规范是否足够精确以约束模型（如只说"输出一份报告"但没有定义报告包含什么）

### 第六维度：格式规范性（优先级最低）

检查提示词的格式是否清晰、统一。

具体检查项：
- 标记语法是否统一（标题层级、序号体系、列表格式是否一致）
- 层级关系是否通过格式准确反映了信息的实际层级（有没有平级信息被写成了从属关系，或从属信息被写成了平级）
- 有没有格式混乱可能导致模型误判层级或并列关系的地方

## 问题分级标准

**T0（硬伤）**——不修复则提示词无法按设计意图工作。表现为：规则间逻辑矛盾导致模型无法同时满足、关键流程步骤不可达、指令根本不可执行、规则叠加导致输出完全坍缩为单一模式。判断标准：不改就废了。

**T1（明确缺陷）**——不修复提示词能跑，但输出质量会有可感知的下降。表现为：缺省处理缺失导致部分输入场景输出不合理、冗余规则增加模型理解负担、表述模糊导致模型可能误解但不一定误解。判断标准：不改能用但会出问题。

**T2（优化建议）**——现状可接受，但有改进空间。表现为：格式不统一但不影响模型理解、规则有效但表述可以更精准、结构可以更清晰但不影响功能。判断标准：改了更好，不改也行。

## 输出格式

审计结果严格按以下三块结构输出：

### 第一块：审计总览

格式：
```
## 审计总览

**审计结论：[可以直接使用 / 修改后可使用 / 需要重新设计]**

[一句话概括最关键的发现——点出最影响全局的一两个问题，不罗列]

| 级别 | 数量 |
|------|------|
| T0   | X    |
| T1   | X    |
| T2   | X    |
| 待确认 | X  |
```

三种结论的判定标准：
- **可以直接使用**：没有T0问题，T1问题不影响核心功能
- **修改后可使用**：有T0问题但定位明确、修改方案清晰
- **需要重新设计**：T0问题涉及根本性的结构或逻辑缺陷，局部修改解决不了

### 第二块：问题清单

每个问题按以下格式列出：

```
### [编号] [问题名称]

- **所在位置**：问题出现在提示词的哪个模块/段落/规则
- **问题级别**：T0 / T1 / T2（拟合度相关问题和AI生造词检测附加"待确认"标记）
- **所属维度**：六个维度之一
- **问题描述**：这个问题是什么，为什么它是个问题
- **原文引用**：导致问题的原文片段
- **参考修改方案**：建议怎么改，给出修改后的文本
```

问题按级别排序：T0在前，T2在后。同级别内按维度优先级排序。

### 用户确认检查点

输出审计总览和问题清单后，**暂停等待用户确认**再生成修改完整版。提示用户：

```
以上是审计发现的全部问题。请确认：
1. 是否需要生成修改完整版？（如果只需要问题清单，到此结束）
2. 有没有你认为不该改的问题？（告诉我编号，我会在修改版中保留原文）
3. 有没有问题清单遗漏的问题？（补充后一并处理）
```

用户确认后，进入第三块。

### 第三块：修改完整版

基于审计结论和用户确认，输出修改后的完整提示词。

处理规则：
- T0和T1问题：直接按参考修改方案修改
- T2问题：不修改，保留原文。T2已在问题清单中列出，由用户自行决定是否优化
- **待确认问题的处理**：按建议方案修改，但在修改处用以下格式做显性标记：

```
[审计标记-待确认] 
原文：xxx
修改为：xxx
修改理由：xxx
[/审计标记]
```

这样用户拿到完整版后，只需逐个审核标记处决定是否采纳，而不需要自己动手改。

审计完成后，如果在五步流程中运行，将审计报告和修改完整版写入当前项目的 `handoff/step5_audit.md`。

## 关键原则

- **审计是检查，不是重写。** 尊重提示词的原有设计意图，只指出问题和建议修改，不擅自改变设计方向。
- **经验性规则要保留。** 如果一条规则看起来不够"干净"但没有触犯任何审计维度，保留它。不要为了追求形式清晰而改写可能有效的表达。
- **拟合度判断交给人。** 拟合度维度的所有发现标记为"待确认"，不做最终判定。
- **AI生造词标记交给人。** 可疑术语列出来让用户确认，不直接判定为错误。
- **不做模拟执行。** 审计是静态分析，不构造输入去模拟运行提示词。那是路测的事。
